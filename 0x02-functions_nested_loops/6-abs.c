#include "main.h"
#include <stdlib.h>

/**
 * _abs - function returns the absolute value of any given input
 * i - input parameter
 * Returns: Always(0)
 */

int _abs(int i)
{
	_putchar(abs(i));
	_putchar('\n');
	return (0);
}
